# This is a config for E2B sandbox template.
# You can use template ID (mz839asfhlfoofwy2dpz) or template name (manus-sandbox-v2) to create a sandbox:

# Python SDK
# from e2b import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("manus-sandbox-v2") # Sync sandbox
# sandbox = await AsyncSandbox.create("manus-sandbox-v2") # Async sandbox

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create('manus-sandbox-v2')

memory_mb = 2_048
cpu_count = 2
dockerfile = "e2b.Dockerfile"
template_name = "manus-sandbox-v2"
template_id = "mz839asfhlfoofwy2dpz"
